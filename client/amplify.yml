version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd client # Navigate to your frontend folder
        - npm ci # Install dependencies
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: client/dist
    files:
      - '**/*'
  cache:
    paths:
      - client/node_modules/**/*
